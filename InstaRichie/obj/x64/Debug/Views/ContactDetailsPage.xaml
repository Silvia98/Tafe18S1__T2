<Page x:ConnectionId='1'
    x:Class="StartFinance.Views.ContactDetailsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:StartFinance.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Template10.Controls"
      xmlns:vm="using:StartFinance.ViewModels"
                        
    mc:Ignorable="d">


    <Page.Resources>
        <DataTemplate x:Key="cdListView">
            <Grid>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding contactID}" Padding="0,0,15,0"/>
                    <TextBlock Text="{Binding firstName}" Padding="0,0,15,0"/>
                    <TextBlock Text="{Binding lastName}" Padding="0,0,15,0"/>
                    <TextBlock Text="{Binding phoneNumber}" Padding="0,0,15,0"/>
                </StackPanel>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Page.DataContext>
        <vm:MainPageViewModel x:ConnectionId='2' x:Name="ViewModel" />
    </Page.DataContext>



    <RelativePanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='3' x:Name="AdaptiveVisualStateGroup">
                <VisualState x:ConnectionId='4' x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for narrow view  -->
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='5' x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for normal view  -->
                    </VisualState.Setters>
                </VisualState>
                <VisualState x:ConnectionId='6' x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <!--  TODO: change properties for wide view  -->
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>


        <controls:PageHeader x:ConnectionId='7' x:Name="pageHeader" RelativePanel.AlignLeftWithPanel="True"
			RelativePanel.AlignRightWithPanel="True"
			RelativePanel.AlignTopWithPanel="True" Text="Contact Details">

            <!--  secondary commands  -->

        </controls:PageHeader>
        <TextBox x:ConnectionId='8' x:Name="firstNTextBox" HorizontalAlignment="Left" Margin="48,120,0,0" TextWrapping="Wrap" Header="First Name" PlaceholderText="" VerticalAlignment="Top" Width="274"/>
        <TextBox x:ConnectionId='9' x:Name="LastNTextBox" HorizontalAlignment="Left" Margin="48,185,0,0" TextWrapping="Wrap" Header="Last Name" PlaceholderText="" VerticalAlignment="Top" Width="274"/>
        <TextBox x:ConnectionId='10' x:Name="phoneTextBox" HorizontalAlignment="Left" Margin="48,250,0,0" TextWrapping="Wrap" Header="Phone Number" PlaceholderText="" InputScope="Number" VerticalAlignment="Top" Width="274"/>
        <ListView x:ConnectionId='11' x:Name="ContactListView" HorizontalAlignment="Left" Height="235" Margin="48,315,0,0" VerticalAlignment="Top" Width="274" ItemTemplate="{StaticResource cdListView}"                                                    />

        <controls:PageHeader x:ConnectionId='12' x:Name="pageFooter" RelativePanel.AlignLeftWithPanel="True"
			RelativePanel.AlignRightWithPanel="True"
			RelativePanel.AlignBottomWithPanel="True" Margin="0,592,0,0">

            <!--  secondary commands  -->
            <controls:PageHeader.PrimaryCommands>
                <AppBarButton x:ConnectionId='14' Icon="Add" Label="Add Account"                            />
                <AppBarButton x:ConnectionId='15' Icon="Edit" Label = "Edit Account"                             />
                <AppBarButton x:ConnectionId='16' Icon="Delete" Label="Delete Account"                          />

            </controls:PageHeader.PrimaryCommands>
        </controls:PageHeader>
        <TextBlock x:ConnectionId='13' x:Name="textBlock" HorizontalAlignment="Left" Height="35" TextWrapping="Wrap" Text="Add Contact Details" VerticalAlignment="Top" Width="272" Margin="51,68,-261.333,-83" FontSize="20"/>

    </RelativePanel>
</Page>

